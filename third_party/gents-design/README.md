Packed `@gents/tokens`, `@gents/ui`, and `@gents/shell` from
`gents-ai/gents-design` for the public gents repo.

- Pin: see `PIN` (design repo commit SHA)
- Refresh: `scripts/refresh-gents-ui.sh` from the gents repo root

