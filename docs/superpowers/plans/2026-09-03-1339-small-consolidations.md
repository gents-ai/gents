# #1339 Small Single-Owner Consolidations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close the checklist in #1339: each item is one fact with two owners and a one- or two-file fix. Batched by crate so each task is one review surface.

**Tech Stack:** Rust, TypeScript.

**Spec:** GitHub issue #1339 (checklist). Items already closed by earlier PRs in the stack are skipped and ticked in the issue with the PR number.

## Global Constraints

- Each item deletes a duplicate; none adds a new abstraction beyond a single shared function or constant.
- Behavior preserved; where a duplicate had already drifted (child-terminal wording), the runtime's `background_tools.rs` wording wins.
- Net code deletion.

---

### Task 1: Runtime batch (`crates/gents`)

- [ ] `document_config/tool_selection.rs:~413` `NATIVE_TOOL_NAMES`: replace with an iterable name registry on `NativeTool` (`NativeTool::ALL_NAMES` or `NativeTool::iter().map(tool_name)`) and delete the literal; keep the drift test but make it assert against the registry.
- [ ] `hook/persistence/helpers.rs:~415` `child_terminal_status`/`child_terminal_error` and `background_completion/rendering.rs:~39` `child_terminal_status`/`child_terminal_summary`: one pair next to `project_child_terminal` in `background_tools.rs`; both callers import it; the `Dead` wording is the `background_tools` one.
- [ ] `hook.rs:~960` and `hook/persistence/approval.rs:~197` deadline-expiry check: one `ToolCallLifecycle::is_deadline_expired(now)`.
- [ ] `tool_call_lifecycle/recovery.rs:~348-480` `reconcile_terminal_parent_owned_tools`: call `classify_running_tool_recovery` instead of the inline reimplementation; document (one comment) why deadline expiry is intentionally excluded from that sweep, or include it if the classifier's deadline branch is correct there (decide from the Lean `Proofs/Recovery/Sweeps/ToolCalls.lean` model; the model treats deadline as part of the classifier).
- [ ] `watcher/query/rows.rs:~84` vs `lifecycle/claim.rs:~462` `valid_until` parsing: one `lifecycle::parse_valid_until(value: Option<&str>, now) -> TtlOutcome { NotSet, Live, Expired, Malformed }`; the watcher treats `Malformed` as not claimable (fail closed, matching the claim path's error) and logs once.
- [ ] Test each with the existing unit tests plus one per item where the brief names a behavior; `cargo test -p gents --lib` green; commit — `runtime: small single-owner consolidations (#1339)`.

### Task 2: CLI batch (`crates/gents-cli`)

- [ ] `config_import.rs:~32` `CONFIG_PRUNE_ORDER`: derive from `CONFIG_APPLY_ORDER` reversed (const fn or a `LazyLock`); keep the Lean-witness test.
- [ ] `gents-chatgpt-login` and `chatgpt_oauth_refresh.rs` OAuth client id / refresh endpoint literals: one `pub mod chatgpt_oauth::constants` in `gents-protocol` (or the login crate) that both import.
- [ ] `cargo test -p gents-cli --lib config_import`, `cargo test -p gents-chatgpt-login`; commit — `cli: derive prune order; one ChatGPT OAuth constants module (#1339)`.

### Task 3: Desktop batch (bridge + TS)

- [ ] `gents-desktop-bridge/src/commands/chat.rs:8-26`: delete the private `turn_state_label`; `can_send_in_turn` = `state.is_terminal()` from `gents_protocol::client_protocol::ClientTurnState`; `snapshot/session/projection.rs:~174` and `live_delta.rs:~118` use one `is_live_turn_state` in `types/util.rs`.
- [ ] `packages/gents-desktop-chat/src/chat-shell.ts:~167` and `packages/gents-desktop-fleet/src/components/FleetRow.tsx:~27` `isTerminalTurnState`: one export from `gents-desktop-client` (`turnState.ts`), both import it; delete the copies.
- [ ] `packages/gents-desktop-fleet/src/fleetMetrics.ts:~78` `needsInferenceSetup`: replace with `behaviorReadinessIsInferenceFailure(selectedBehaviorReadinessDecision(deployment, null))` from `operationalState.ts`; delete the raw-field check; update `FleetRow.tsx`/`FleetDashboard.tsx` callers and tests.
- [ ] `packages/gents-desktop-fleet/src/peerConnectionErrors.ts:~9-50`: branch on `BridgeInvokeError.code === "endpointUnreachable"`; the bridge attaches the endpoint as a structured field on `BridgeError` (`error.rs:64-119` already extracts it from the message) so the regex `requestUrlFromMessage` is deleted; contract MINOR bump if the error view gains a field, hash regenerated by the crate test.
- [ ] `cargo test -p gents-desktop-bridge`, `npm run build:packages && npm run test:packages`, `cd apps/gents-desktop && npx vitest run`; commit — `desktop: turn-state, inference-setup, and endpoint-error single owners (#1339)`.

### Task 4: Gate and issue bookkeeping
- [ ] `cargo check --workspace --all-targets`, `cargo fmt --all --check`; net deletion check.
- [ ] Tick each checklist item in #1339 with a comment naming the PR; items closed earlier in the stack are ticked with that PR's number.
