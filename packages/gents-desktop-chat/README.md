# @source-inc/gents-desktop-chat

Headless chat workflow projection (`projectChatShell`, turn/send state) and
`useMasterDetail` for host shells. Presentational components extract in a
follow-up; Gents Desktop remains the reference composition.

**Required bridge grants:** default + session-read + chat-write + resend-control + interrupt-read + interrupt-control.
