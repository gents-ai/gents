# @source-inc/gents-desktop-fleet

Fleet metrics, peer connection error formatting. `BrandLockup` stays host-owned via a brand slot on presentational surfaces.

**Required grants:** default + fleet-read; add fleet-admin for pairing UI.
