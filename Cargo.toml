[workspace]
resolver = "2"
members = [
    "apps/desktop-tauri/src-tauri",
    "crates/defra-agent",
    "crates/defra-agent-cli",
    "crates/defra-agent-desktop",
    "crates/defra-agent-desktop-core",
    "crates/defra-agent-lean-contract",
    "crates/defra-agent-lenses/agent_tool_call_lifecycle_v1_to_v2",
    "crates/defra-agent-lenses/agent_subagent_v2_to_v3",
    "crates/defra-native-fs-runner",
    "crates/defra-agent-protocol",
]
# Exclude the WASM lens crate from default workspace builds. Its cdylib target
# relies on `lens_sdk::define!` which imports an `extern "C" fn next()` only
# resolvable at WASM-load time by the lens host runtime. Build it explicitly
# via `cargo build -p agent-tool-call-lifecycle-v1-to-v2-lens --target
# wasm32-unknown-unknown`. The rlib target (used by tests via the
# default-features=false dev-dep) is unaffected.
default-members = [
    "apps/desktop-tauri/src-tauri",
    "crates/defra-agent",
    "crates/defra-agent-cli",
    "crates/defra-agent-desktop",
    "crates/defra-agent-desktop-core",
    "crates/defra-agent-protocol",
]

[workspace.package]
version = "0.1.5"
edition = "2021"
license = "MIT"
repository = "https://github.com/sourcenetwork/defra-agent"

[workspace.dependencies]
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive", "env"] }
crypto = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
defra-core = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
defra-agent-protocol = { path = "crates/defra-agent-protocol" }
defra-agent-desktop-core = { path = "crates/defra-agent-desktop-core" }
defra-agent-lean-contract = { path = "crates/defra-agent-lean-contract" }
defra-node = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
defra-p2p-adapter = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
dirs = "5"
events = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
hostname = "0.4"
identity = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
minijinja = { version = "2", default-features = false, features = ["builtins", "serde"] }
opentelemetry = { version = "0.31", default-features = false, features = ["trace"] }
opentelemetry-otlp = { version = "0.31.1", default-features = false, features = ["trace", "http-proto", "reqwest-client"] }
opentelemetry_sdk = { version = "0.31", default-features = false, features = ["trace"] }
p2p = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
query = { git = "ssh://git@github.com/sourcenetwork/defradb.rs.git", tag = "v0.13.0" }
rand = "0.9"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
rig-core = "0.35.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
tokio = { version = "1", features = ["full"] }
tokio-util = "0.7"
toml = "0.8"
tracing = "0.1"
tracing-opentelemetry = { version = "0.32.1", default-features = false }
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1", features = ["v4"] }

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
strip = true
overflow-checks = true
debug = "line-tables-only"

[profile.dev-install]
inherits = "release"
lto = "thin"
codegen-units = 16
strip = false

[profile.dev.package."*"]
opt-level = 1

[profile.test]
opt-level = 1
debug = 0
split-debuginfo = "off"
incremental = false
