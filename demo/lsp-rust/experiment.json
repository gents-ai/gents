{
  "name": "lsp-rust",
  "description": "Least-privilege lsp tool against rust-analyzer on the Gents workspace. Not a CI gate.",
  "init": {
    "inference_url": "${GENTS_EXP_ENDPOINT:-http://100.73.235.38:8000/v1}",
    "model_name": "${GENTS_EXP_MODEL:-d4f}",
    "backend_preset": "vllm",
    "openai_wire_api": "chat-completions"
  },
  "seed": {
    "collection": "LspDemoJob",
    "job_id_field": "job_id",
    "prompt_field": "prompt"
  },
  "default_prompt": "This is the Gents repository. Use the lsp tool only; do not guess. Hover CommandNetworkMode::meet in crates/gents/src/toolset/shared/command.rs and quote the documented rank order. Hover lsp_advertised in crates/gents/src/toolset/lsp/auth.rs and quote the signature. Then call lsp status. Reply DONE.",
  "expect": {
    "trigger_ids": ["lsp-hover"]
  },
  "await_timeout_secs": 300
}
