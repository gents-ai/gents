Execute the closed maintenance plan for run {{ event.correlation }}. The report declares {{ doc.work_package_count }} ordered work packages:

{{ doc.summary }}

You are the single execution owner. Package creation events are not ordered callbacks, so never wait for another executor and never assume document arrival order.

1. Call `defra_query` for `MaintenanceWorkPackage` in this run. Require exactly {{ doc.work_package_count }} unique packages. Parse `sequence` numerically, sort ascending, and require the exact contiguous range 1..N; one shared repository, absolute worktree parent, immutable base, PR base, branch, absolute worktree path, and expected total; and an exact `previous_package_id` chain. Require the worktree path to be a direct child of the declared worktree parent and not inside the source repository. If the ledger is incomplete or inconsistent, write one `blocked` result for every package that can be identified, then write one blocked `MaintenanceExecutionSummary`. Do not edit.
2. Call `defra_query` for `MaintenanceExecutionResult` in this run before editing. If a complete, internally consistent ledger already exists, verify its commits and close it with the summary instead of duplicating work. If partial results exist, resume only when they form a clean completed prefix whose recorded commits exactly match the shared worktree branch; otherwise close blocked. Never retry or overwrite a terminal package result.
3. If the only package has `finding_ids` exactly `none`, write its result as `skipped`, create no worktree or commit, then write a skipped summary and stop.
4. Confirm the source resolves the immutable base, the shared branch does not exist, and the target path does not exist. Create the shared worktree once with the repository's documented helper. For Gents: `make worktree BRANCH=<shared-branch> DIR=<shared-worktree-path> BASE=<immutable-base>`.
5. Process packages strictly in numeric order. Before each package, require the worktree to be on the shared branch, contain the previous recorded commit, and have no staged or unstaged changes. Re-read repository instructions. Implement only that package; a package may span code areas, but its commit must remain a focused, reviewable unit and must not absorb opportunistic cleanup.
6. Run the package validation and every repository gate proportional to the touched boundary. For Gents, use the full `cargo test -p gents` suite and `cargo check --workspace --all-targets` where required; never substitute `--lib`.
7. Inspect the complete staged diff, stage only in-scope files, and create exactly one descriptive commit. Do not amend or rewrite an earlier unit. Confirm the worktree is clean and capture the new commit SHA.
8. Call `write_maintenance_execution_result` exactly once for that package before advancing. Use status `completed` only for a validated new commit. Preserve package ID, sequence, branch, and worktree path; report compact changed paths and exact validation. Do not supply runtime-filled `run_id` or `expected_total`.
9. If any package cannot safely complete, write its result as `blocked`, do not create a partial commit, stop executing later packages, and write blocked results for the unattempted suffix with the predecessor failure as evidence.

After every planned package has exactly one terminal result, call `write_maintenance_execution_summary` exactly once. Set status `completed` only when all N executable packages completed with distinct commits, the branch contains them in order, and the worktree is clean. Use `skipped` only for the sentinel. Otherwise use `blocked`. Counts must balance exactly to N. Record the final commit SHA, or an empty string for skipped/no-commit execution. Do not supply runtime-filled `run_id` or `expected_total`.
