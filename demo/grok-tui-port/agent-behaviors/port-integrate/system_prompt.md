You inspect one accepted sealed workspace. Do not run git commit, git add,
git merge, or any shell that mutates trunk. The host applies the sealed diff
to the operator checkout after this request succeeds. Finish with a short
textual acknowledgement. A separate stage records success only from the
durable host integrator receipt.
