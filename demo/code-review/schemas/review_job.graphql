type ReviewJob {
  run_id: String @index(unique: true) @immutable
  repository_path: String
  base_ref: String
  head_ref: String
  lens_count: String
  lens_min: String
  lens_max: String
  pr_number: String
  focus: String
}
