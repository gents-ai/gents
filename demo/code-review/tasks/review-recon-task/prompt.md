Review job {{ event.correlation }} targets the Gents repository at `{{ doc.repository_path }}` with focus: {{ doc.focus }}.

Review `{{ doc.base_ref }}...{{ doc.head_ref }}`. Resolve the merge base explicitly and keep every proposed finding attributable to that diff. Record whether the working tree is dirty, but do not silently add uncommitted changes to the PR boundary.

GitHub PR number (optional): `{{ doc.pr_number }}`. When it is non-empty, use read-only `gh pr view`, `gh pr diff`, and `gh pr checks` calls to understand the stated intent, linked issues, remote head SHA, changed-file list, and CI state. Confirm the local head matches the PR before treating local build results as PR evidence. Never comment, approve, edit, close, merge, or otherwise mutate the PR.

Lens policy: `{{ doc.lens_count }}`. If it is a positive integer, create exactly that many review lenses. If it is `auto`, choose the smallest adequate count between {{ doc.lens_min }} and {{ doc.lens_max }} after inspecting the diff, build results, affected architectural boundaries, and PR intent. Use the chosen count as `expected_total` on every area; this immutable count closes the fan-in set. A narrow localized change should stay near the lower bound, while cross-cutting runtime, persistence, authorization, provider-boundary, trigger, or formal-model changes justify more independent lenses.

Recon decomposes the review; it does not perform every lens itself. Combine related Git/GitHub queries and file reads instead of paging repeatedly through the same diff. Start both required Cargo commands with `spawn_process` before deep repository inspection; do not run them as foreground Bash calls. Inspect enough context to name precise invariants and entry points, then delegate deep call-path proof and defect verification to the scanners. Once the baseline processes finish and the evidence packets are self-contained, finalize the closed lens list and write it immediately. Do not serially complete work that belongs to the parallel scanners.

Run the deterministic baseline first from the repository root: identify all changed files and the exact diff from the merge base of `{{ doc.base_ref }}` and `{{ doc.head_ref }}`, read the workspace edition/MSRV, and start `cargo check --workspace --all-targets` plus `cargo clippy --workspace --all-targets --all-features -- -D warnings` with the background process tools so repository inspection can continue while they run. Collect both results before writing areas. Record concise pass/fail output; compiler and Clippy diagnostics are authoritative and must not later be duplicated as model findings.

Detect review technologies in Cargo manifests and the diff: request/process/persistence transitions, Tokio concurrency and cancellation, DefraDB document identity/ACP/GraphQL boundaries, provider-input sanitization, tool and subagent authorization, recovery/trigger convergence, unsafe/FFI/atomics, macros, desktop/CLI adapters, and changed tests or Lean proofs. The runtime's integrity wins over peripheral convenience. A change to legal transitions, invariants, or provider input should be matched by Lean model and conformance coverage; do not demand Lean changes for plumbing or tooling. Locate the existing implementation owner before accepting a new subsystem or second code path. DefraDB is the control plane and a full database, not a thin document store: inspect the pinned `defradb` dependency features in the workspace and its available API/source evidence before recommending that Gents recreate querying, indexing, identity, ACP, persistence, replication, subscription, or transaction behavior beside it.

Choose the complete set of genuinely different review lenses from what is detected, not path partitions. Core Rust correctness and architecture/reuse are always represented. Do not add lenses merely to reach the automatic upper bound; each lens must own distinct invariants and evidence. Use `lsp` selectively to resolve definitions or cross-file references for important changed symbols where plain text could confuse shadowing or re-exports. With a relative file and known symbol, omit `line` to let Gents locate it or provide the exact 1-indexed line plus `symbol`; use `references` for consumers, `definition`/`implementation` for ownership, `hover` for types, and `symbols` for a file outline or workspace query. Read the returned locations in full context. Do not call rename, code-action apply, or other mutation actions. Check every new cross-boundary type, variant, message, event, command, or queue value at both its producer and its consuming dispatcher; silent drops often sit outside the diff.

Check the repository-specific sharp edges: GraphQL interpolation must use `graphql::escape_graphql_string()`, empty DefraDB mutation arrays must be emitted as `null`, and new required Rust fields must build across all workspace targets. The architecture/reuse lens should also identify concrete semantic clutter introduced by the diff: duplicate pathways, compatibility shims without a current caller, narration comments that restate code, stale implementation-history comments, and documentation that duplicates rather than links to the canonical source. Do not flag concise rationale, invariants, safety arguments, operator contracts, or formal-design documentation merely because it is detailed. Before calling `write_review_area`, finalize all lenses and their chosen count. For each lens, create a self-contained evidence packet in `instructions`: name the invariants to check and include several exact, compact `path:line` excerpts from the changed diff. Write one row per lens with `repository_path` exactly `{{ doc.repository_path }}`, the same baseline summary, and the same chosen `expected_total`. Set each unique `area_id` to `{{ event.correlation }}:<lens-slug>` so tool retries cannot duplicate a closed-set member. Never change the count after the first write. Keep each tool call bounded enough for durable storage: `baseline` at most 2,000 characters, `instructions` at most 8,000 characters, and `path` at most sixteen comma-separated paths.
