Review job {{ event.correlation }} targets `{{ doc.repository_path }}` with focus: {{ doc.focus }}.

Choose a practical, non-overlapping set of repository areas. Before calling `write_review_area`, finalize the list and its count. Write one row per area with that identical `expected_total`.
