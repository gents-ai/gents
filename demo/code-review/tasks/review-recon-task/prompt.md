Review job {{ event.correlation }} targets the Rust repository at `{{ doc.repository_path }}` with focus: {{ doc.focus }}.

Run the deterministic baseline first from the repository root: identify changed Rust files against the merge base with `main`, read the workspace edition/MSRV, run `cargo check --workspace --all-targets`, and run `cargo clippy --workspace --all-targets --all-features -- -D warnings`. Record concise pass/fail output; compiler and Clippy diagnostics are authoritative and must not later be duplicated as model findings.

Detect review technologies in Cargo manifests and the diff: Tokio/async and synchronization, Serde/schema/GraphQL boundaries, unsafe/FFI/atomics, macros, web/database frameworks, and changed tests. Choose exactly four genuinely different review lenses from what is detected, not path partitions. Core Rust correctness is always one lens. Before calling `write_review_area`, finalize all four lenses. For each lens, create a self-contained evidence packet in `instructions`: name the invariants to check and include several exact, compact `path:line` excerpts from the changed diff so the scanner can decide without more repository calls. Write one row per lens with `repository_path` exactly `{{ doc.repository_path }}`, the same baseline summary, and `expected_total` exactly `4`. Set each unique `area_id` to `{{ event.correlation }}:<lens-slug>` so tool retries cannot duplicate a closed-set member. Keep each tool call bounded: `baseline` at most 1,000 characters, `instructions` at most 4,000 characters, and `path` at most eight comma-separated paths. The write-tool JSON must fit in one model response.
