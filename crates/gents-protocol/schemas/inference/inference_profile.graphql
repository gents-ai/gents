type InferenceProfile @branchable @index(fields: ["agent_did", "profile_id"], unique: true) {
    profile_id: String
    agent_did: String @index
    display_name: String
    description: String
    backend_id: String @index
    model_name: String
    reasoning_effort: String
    context_window: Int
    max_output_tokens: Int
    sampling_id: String @index
    execution_id: String @index
    tags: [String!]
    updated_at: DateTime @index(direction: DESC)
}
