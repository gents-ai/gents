type GraphDefinition @branchable {
    graph_id: String @index(unique: true) @immutable
    owner_did: String @index @immutable
    enabled: Boolean
    active_revision_digest: String @index
    generation: Int
    created_at: DateTime @index(direction: DESC) @immutable
    updated_at: DateTime @index(direction: DESC)
}
