Build a small, polished voxel-art pagoda in `{{USER_HOME}}/pagoda`.

Create a self-contained `index.html` that opens locally without fetching any dependencies. Show a multi-tier pagoda with stepped roofs, a garden, and a coherent voxel aesthetic. Include an accessible button labelled "Toggle night" that switches between day and night, a visible on-page title, and an HTML document title. Choose the implementation and visual details yourself.

Use your configured tools to create the project, run meaningful local checks, and fix any problems you find. Include a short `README.md` explaining how to open it and what you checked. Keep all project files within the pagoda folder, containing only regular deliverable files: no installed dependencies, caches, or symlinks; fewer than 10,000 entries and at most 16 MiB per file. Report the resulting files and verification honestly.
