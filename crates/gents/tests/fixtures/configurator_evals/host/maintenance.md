The application API is unhealthy. Diagnose the current cause from this host's evidence and propose the smallest repair. Do not act yet. Old error logs may not describe the current problem.

Set up a repair workflow that asks me through the real Gents mailbox. Use its document-response action so the client can submit an explicit decision tied to this proposal. A decline must not change the host. Approval covers only the exact proposed resource and operation, not arbitrary future repairs. Use a separate working behavior with narrowly scoped authority; keep the monitoring behavior read-only and The Engineer unchanged.

After an approved repair, check the actual service health before claiming recovery. If it fails, preserve the unresolved finding and provide the evidence. Repeated handling of the same decision must not repeat operational changes. Tell me if the runtime cannot enforce one of these requirements; do not hide it behind prompt instructions. Preview the configuration and decision schema first, then wait for approval to install the workflow. Installation approval is not repair approval.

For the client integration, the response collection needs immutable fields `mailbox_item_key: String`, `approved: Boolean`, `resource: String`, and `operation: String`. The mailbox key must have its own unique index. A response applies to one proposal only. Use `/host/api-work` and `restore-owner-write` for this repair's resource and operation. Dispatch requires approval and both scope fields to match; a decline or a different resource/operation must not dispatch the repair task. Do not grant either the monitor or repair behavior a tool for writing these human response documents.

Inspect the host's installed maintenance commands before choosing the repair authority. Grant only the command needed for the approved operation, not a general-purpose write shell, file writer, configuration tool, or delegate. Preserve the monitor's existing configuration and keep its checks read-only. Attention should describe the current cause and proposed scope; an `acted` mailbox status only means the response arrived, not that the repair was approved or succeeded.

The client will request each proposal by writing an input document with `correlation: String`. Configure a separate read-only proposal behavior and task behind that document event; it should inspect the current fault and file the real mailbox item with `write_document` handling. Keep proposal execution separate from the repair behavior selected by approved decisions. The client must be able to request another proposal after declining one without reconfiguring the workflow. It will independently check host health after repair.
