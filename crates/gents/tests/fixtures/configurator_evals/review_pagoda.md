Review the project in `{{USER_HOME}}/pagoda` against this request: a self-contained voxel-art pagoda with multiple tiers, a garden, a visible title, and an accessible "Toggle night" button that switches day/night without network dependencies.

Read the actual files. Identify concrete correctness, usability, and accessibility problems, with file references and practical fixes. Use your read-only tools and leave the files unchanged. Distinguish observed defects from suggestions; do not invent findings to fill a quota.
