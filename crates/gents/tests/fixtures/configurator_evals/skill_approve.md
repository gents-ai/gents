I approve importing exactly {{SKILL_DIRECTORY}} as skill ID eval-coding-check and attaching that skill to the existing Builder behavior. Apply those changes now, preserving Builder's other skills, context, root and tool settings and leaving Setup unchanged. This approval covers only that import and attachment, not executing the skill's procedure. Verify the persisted source directory and attachment. If the attachment preview cannot resolve the skill before import, import the approved skill first, then preview and apply the attachment.
