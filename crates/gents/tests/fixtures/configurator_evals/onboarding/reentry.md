I am running setup again. Inspect the current canonical configuration before acting.

Ensure there is exactly one enabled behavior named `Onboarding Builder`, still using profile `onboarding-medium`, the write preset, and root `{{USER_HOME}}`. Preserve its current system prompt byte-for-byte, including every user-authored edit. Keep it as the default. Do not clone it, create a replacement, or alter Setup, backends, credentials, grants, tools, or profiles. Use previews before any write; if the requested state already exists, make no write.
