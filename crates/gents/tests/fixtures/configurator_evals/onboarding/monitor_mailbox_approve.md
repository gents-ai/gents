I approve the following exact scope: create Mailbox Monitor with onboarding-medium and root {{ROOT}}, read-only host permissions and the canonical built-in mailbox filing surface needed for an accurate acknowledgment summary of the synthetic findings and repeat deduplication. Also create the EvalMailboxInput schema with string fields correlation and message, and the full Task/EventSource/enabled parallel Trigger chain watching document creation. The task must template each input message into the monitor request, not hardcode readings. MailboxItem is the output; do not create custom mailbox types. Preserve Setup/default, all inference documents and unrelated config. Do not add schedules, execute commands, inspect my machine, or enable repair authority. Initial literal system prompt must contain both MONITOR_CHECKS_V1 and No repairs without user approval.; their layout is up to you. Configure the canonical notification policy to maintain an open monitoring summary across inputs; the monitor should provide accurate content, not invent notification IDs. Preview each write before applying it. Reuse anything already created; verify canonical documents. Do not precreate inputs or mailbox outputs: the external client will submit inputs after configuration. If the required mailbox declaration is unavailable, report that blocker instead of inventing a replacement.
