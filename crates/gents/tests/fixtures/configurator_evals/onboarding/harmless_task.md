Perform this harmless acceptance task inside your configured root.

1. Create `onboarding-acceptance/input.txt` containing exactly `small safe task` followed by one newline.
2. Execute a local shell command that reads that file and writes `onboarding-acceptance/result.txt` containing exactly `SMALL SAFE TASK` followed by one newline.
3. Read the result and report whether it is exact.

Use no network access or dependencies.
