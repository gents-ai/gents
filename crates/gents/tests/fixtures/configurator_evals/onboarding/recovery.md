Recover from the previous rejected request using only the authority already available. Use previews before writes.

Create exactly one enabled behavior named `Recovered Builder` using profile `onboarding-low`, the write preset, and root `{{USER_HOME}}`. Its system prompt should tell it to perform small local coding tasks. Do not change the default yet. Keep Setup and `Onboarding Builder` intact, and do not create backends, credentials, datastore grants, MCP services, or subagent grants.
