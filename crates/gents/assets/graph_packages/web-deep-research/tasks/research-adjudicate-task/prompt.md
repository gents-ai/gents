The closed investigation group for research run {{ group.correlation_value }} contains {{ group.count }} typed closure documents. Group completeness: {{ group.complete }}.

Read every `WebResearchInvestigation`, `WebResearchSource`, `WebResearchClaim`, and `WebResearchEvidence` for this run through the dedicated correlation-scoped tools. Confirm that the closure count is {{ group.count }}, every closure reports the same `expected_total`, and every assignment is represented once. Derive all ledger and verdict counts from the documents you loaded. Reconcile each closure's typed shortfall, engine-degradation, and retrieval-failure diagnostics, and reject source links without an integrity-verified handoff. Treat mismatches or broken claim-to-source joins as explicit evidence gaps; never silently infer missing handoff data. Source records are authoritative for fetch IDs and hashes. Write exactly one `WebResearchClaimVerdict` for every claim document, including duplicates, and copy its statement exactly. Keep verdict prose concise, write no more than six verdicts per model turn, and never retry a successful verdict write. Accept exact quotations only when a typed evidence link and its source carry the same gateway-verified excerpt; you have no web tools and must not retry verification. Then write one `WebResearchDraft` as the final write without model-authored verdict totals. Do not supply runtime-filled `run_id`.
