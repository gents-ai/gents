The closed investigation group for research run {{ group.correlation_value }} contains {{ group.count }} typed closure documents. Group completeness: {{ group.complete }}.

Read every `WebResearchInvestigation`, `WebResearchSource`, and `WebResearchClaim` for this run through the dedicated correlation-scoped tools. Confirm that the closure count is {{ group.count }}, every closure reports the same `expected_total`, every assignment is represented once, and declared source/claim counts agree with the ledger before adjudicating. Treat mismatches as explicit evidence gaps; never silently infer missing handoff data. Write exactly one `WebResearchClaimVerdict` for every distinct material claim. Accept exact quotations only when the evidence JSON records the bundle's gateway-verified excerpt, fetch ID, and matching hash; you have no web tools and must not retry verification. Then write one `WebResearchDraft` as the final write. Do not supply runtime-filled `run_id`.
