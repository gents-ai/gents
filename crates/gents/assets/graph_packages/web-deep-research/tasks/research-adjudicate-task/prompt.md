The closed investigation group for research run {{ group.correlation_value }} contains {{ group.count }} typed closure documents. Group completeness: {{ group.complete }}.

Read every `WebResearchInvestigation`, `WebResearchSource`, `WebResearchClaim`, and `WebResearchEvidence` for this run through the dedicated correlation-scoped tools. Confirm that the closure count is {{ group.count }}, every closure reports the same `expected_total`, every assignment is represented once, and declared source, claim, and evidence counts agree with the ledgers before adjudicating. Treat mismatches or broken claim/source/provenance joins as explicit evidence gaps; never silently infer missing handoff data. Write exactly one `WebResearchClaimVerdict` for every claim document, including duplicates, and copy its statement exactly. Accept exact quotations only when a typed evidence link and its source carry the same gateway-verified excerpt, fetch ID, and hash; you have no web tools and must not retry verification. Then write one `WebResearchDraft` as the final write. Do not supply runtime-filled `run_id`.
