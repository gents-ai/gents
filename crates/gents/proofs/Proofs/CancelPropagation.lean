import Proofs.Conformance.ContractCases.CancelPropagation

/-!
# Cancel Propagation

Top-level conformance home for declarative subagent cancel propagation. The
executable witness lives under `Conformance.ContractCases.CancelPropagation` so
the generated Rust contract snapshot can consume it.
-/
