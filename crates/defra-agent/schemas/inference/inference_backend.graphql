type InferenceBackend {
    backend_id: String @index(unique: true)
    name: String @index
    endpoint: String
    max_concurrent: Int
    enabled: Boolean @index
    models: [String]
    last_probe: DateTime
    probe_status: String @index
}
